-- ============================================================
--  慢慢變好 — 兌換照片 AI 留言
-- ============================================================
--  在 Supabase SQL Editor 整段貼上、按 Run。安全、可重複執行。
-- ============================================================

alter table public.reward_history
  add column if not exists ai_comment text;
