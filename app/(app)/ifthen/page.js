import ComingSoon from "@/components/ComingSoon";

export default function IfThenPage() {
  return (
    <ComingSoon
      title="If → Then 規則 💫"
      subtitle="讓好習慣自動發生的功能正在路上"
      note="第二階段就會加上：建立「IF 觸發 → THEN 行動」的條件反射系統，降低意志力消耗。"
    />
  );
}
